# base-mais-server-setup - DevOps Dependencies
# Установка: pip install -r requirements.txt

# CIS конфигурация (YAML)
pyyaml>=6.0

# REST API (backup.py)
requests>=2.31.0

# Системные пакеты (устанавливаются отдельно):
# restic  — резервное копирование (apt install restic)
# rclone  — Yandex Disk backend для restic (apt install rclone)
